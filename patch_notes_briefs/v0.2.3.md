# v-0.2.3 - minor update

- [v-0.2.3 - minor update](#v-023---minor-update)
  - [News](#news)
    - [Speed wind VFX](#speed-wind-vfx)
    - [Crouch input mode settings](#crouch-input-mode-settings)
  - [Changes](#changes)
    - [Double jump propeller input rework](#double-jump-propeller-input-rework)
    - [Slam input rework](#slam-input-rework)
  - [Fixes](#fixes)
    - [Damage multiplier cleanse](#damage-multiplier-cleanse)
    - [Spawning clipping](#spawning-clipping)
    - [Jump pad](#jump-pad)

## News

### Speed wind VFX

There's a new shader that simulates wind trails on the player's screen under certain circunstances - currently - according to his speed, as well as when he performs a ledge vault/superglide.

### Crouch input mode settings

It is now possible to configure the crouch input mode, for toggle or hold mode.

## Changes

### Double jump propeller input rework

The previous bind mode was unintuitive.

To do a double jump, you had to press jump right before pressing a dash input.

This was designed to avoid conflict between multiple jump-related intentions, but it wasn't a great solution.

The jump+dash input mode is still available, but now, the player can pick among 3 modes :
- jump + dash, as previously
- jump fallback + height - the double jump uses the same jump buffer as other jump related abilities, but it is the last consumer, and will only consume it if the player is above a certain minimum height to avoid bhop conflict.
- dedicated input - simply setup a dedicated input.

The user can mix different modes, as he wishes, although jump + dash and jump fallback + height can't really be used together.

You can find the option in the controls section of the settings, in its general subsection.

### Slam input rework

Same as for the double jump, essentially.

The slam was using the combination of crouch and dash as a trigger input, which can be unintuitive.

Now, there's three configurable modes :
- Dedicated input, for an explicit input to be used
- Quick crouch/tap crouch - triggers when the crouch is quickly started and stopped, that is a quick tap of the input in hold crouch mode, and a double tap in toggle mode.
- Dash+Crouch, the old method, that triggers when dash is pressed right after a crouch input.

## Fixes

### Damage multiplier cleanse
- The damage multipliers were not cleansed properly on run reset, letting the player carry on a mutliplier gathered in a previous run. 

### Spawning clipping
With the new position-specific spawning behavior, enemies could spawn at the exact same position at the exact same time, resulting in weird physics soup.

### Jump pad
The dash + jump pad bug is gone. You no longer preserve the momentum of the dash by dashing into a jump pad.